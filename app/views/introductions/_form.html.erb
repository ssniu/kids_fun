<%= simple_form_for(@introduction, html: {class: "form-horizontal"}) do |f| %>
  <% if @introduction.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@introduction.errors.count, "error") %> prohibited this introduction from being saved:</h2>

      <ul>
      <% @introduction.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.input :title %>
  <%= f.input :description %><br>
  <%= f.input :image_url %><br>
  <%# <%= f.input :user_id, collection: User.all, label_method: :user_id %> 

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
